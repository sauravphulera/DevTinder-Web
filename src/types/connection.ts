export interface Connection {
  _id: string;
  firstName: string;
  lastName: string;
  age: number;
  gender: string;
  photoUrl: string;
  skills: string[];
  createdAt: string;
  requestId: string;
}
